export type Category = { id: number; name: string };

export type PopularPost = {
  id: number;
  title: string;
  date: string;
  image: string;
};

export type Archive = { id: number; month: string };

export type Tag = { id: number; name: string };